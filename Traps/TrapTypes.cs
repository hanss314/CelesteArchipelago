namespace Celeste.Mod.CelesteArchipelago
{
    public enum TrapType
    {
        // Based on offset from UUID, if you are adding a trap use references from this enum
        THEO_CRYSTAL = 0,
        BADELINE_CHASERS = 1,
        SEEKER = 2,
        STAMINA = 3
    }
}